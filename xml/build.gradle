shadowJar.enabled = true

dependencies {
    compile dependencyVersion('jackson.dataformat', 'jackson-dataformat-xml')
    
    compileOnly project(":http")
    compileOnly project(":inject")
    compileOnly project(":http-server-netty")
    compileOnly project(":inject-java")

    testCompile project(":inject-groovy")
    testCompile project(":http-client")
    testCompile project(":runtime")
    testCompile project(":http-server-netty")
}
