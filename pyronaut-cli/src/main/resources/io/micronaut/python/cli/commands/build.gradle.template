plugins {
   id("java")
}

repositories {
%REPOSITORIES%
}

dependencies {
%DEPENDENCIES%
}

tasks.register("resolvePyronautDependencies", Sync) {
    from(configurations.runtimeClasspath)
    destinationDir = file("%DESTINATION_DIR%")
}
