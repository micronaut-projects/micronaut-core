dependencies {
    compileOnly project(":inject-java")
    compile dependencyVersions.rxJava2
    compile project(":http-netty")
    compile project(":runtime")
    compile dependencyVersions.nettyHandlerProxy
    compileOnly dependencyVersions.reactor

    testCompile project(":inject-groovy")
    testCompile project(":inject-java")
    testCompile dependencyVersions.rxJava2
    testCompile dependencyVersions.reactor
    testCompile project(":http-server-netty")
}