dependencies {
    compile dependencyVersions.spring
    compile "org.springframework:spring-tx:$springVersion"
    compile dependencyVersions.springContext
    compile project(":inject")
    compileOnly project(":inject-java")
    compile project(":aop")
}
