`@Refreshable` scope is a custom scope that allows a bean’s state to be refreshed via:

- `/refresh` endpoint.
- Publication of a `RefreshEvent`.

The next example, illustrates the `@Refreshable` scope behavior.

[source,groovy]
----
include::{testsdir}/scopes/RefreshEventSpec.groovy[tags=wheatherController, indent=0]

include::{testsdir}/scopes/RefreshEventSpec.groovy[tags=wheatherService, indent=0]
----

<1> `WeatherService` is annotated with `@Refresable` scope. The publication of a `RefreshEvent` causes its destruction.
<1> `WeatherController.evict` method publishes `RefreshEvent` in the Application Context.

If you invoke the `/weather/forecast` twice, you will see identical responses.

:url: http://localhost:8080
[source, bash, subs="attributes"]
----
include::{testsdir}/scopes/RefreshEventSpec.groovy[tags=forecastCurlCommand, indent=0]
----
returns:
[source, json]
----
{
    "forecast": "Scattered Clouds 01/Feb/18 10:29.199"
}
----

[source, bash, subs="attributes"]
----
include::{testsdir}/scopes/RefreshEventSpec.groovy[tags=forecastCurlCommand]
----
returns
[source, json]
----
{
    "forecast": "Scattered Clouds 01/Feb/18 10:29.199"
}
----

If you invoke the `/weather/evict` endpoint, the `WeatherService` beans gets destroyed.

[source, bash, subs="attributes"]
----
include::{testsdir}/scopes/RefreshEventSpec.groovy[tags=evictCurlCommand]
----
[source, json]
----
include::{testsdir}/scopes/RefreshEventSpec.groovy[tags=evictResponse]
----

The next time you invoke the `/weather/forecast` endpoint, the `WeatherService` is instantiated which leads to a different
response.

[source, bash, subs="attributes"]
----
include::{testsdir}/scopes/RefreshEventSpec.groovy[tags=forecastCurlCommand]
----
[source, json]
----
{
    "forecast": "Scattered Clouds 01/Feb/18 10:33.199" <1>
}
----

<1> Date has changed.