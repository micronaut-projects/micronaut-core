The easiest way to see changes made to a fresh new app for a given profile between Micronaut versions is to produce a clean application using the older and newer version of the `mn` CLI, and then comparing those directories.
