Micronaut ships with several built-in link:{api}/io/micronaut/core/convert/TypeConverter.html[TypeConverter]s.

The next example shows how to use one of the built-in converts (Map to an Object) or create your own.

Given this POGO:

[source, groovy]
----
include::{testsdir}/config/converters/Book.groovy[tag=book,indent=0]
----

and a link:{api}/io/micronaut/context/annotation/ConfigurationProperties.html[ConfigurationProperties]

[source, groovy]
----
include::{testsdir}/config/converters/BookConfigurationProperties.groovy[indent=0]
----

We can use the built-in link:{api}/io/micronaut/jackson/bind/MapToObjectConverter.html[MapToObjectConverter]
and a custom `LocalDateConverter` to convert from configuration to objects.

[source, groovy]
----
include::{testsdir}/config/converters/BookSpec.groovy[tag=bookSpec,indent=0]
----

<1> It converts from Map to `Book` using the built-in link:{api}/io/micronaut/jackson/bind/MapToObjectConverter.html[MapToObjectConverter]
<2> it converts from Map to `LocalDate` using the custom `LocalDateConverter`

[source, groovy]
----
include::{testsdir}/config/converters/LocalDateConverter.groovy[indent=0]
----



