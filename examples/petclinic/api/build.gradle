dependencies {
    compile "org.particleframework:http:${projectVersion}"
    compile "org.particleframework:runtime:${projectVersion}"
    compile "org.particleframework:validation:${projectVersion}"
}
compileJava {
    options.compilerArgs += ["-parameters"]
}
