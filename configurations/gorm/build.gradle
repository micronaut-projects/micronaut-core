dependencies {
    compile dependencyVersions.groovy
    compileOnly project(":inject-java")
    compile project(":inject")
    compile project(":spring")

    compile dependencyVersions.gormDatastore
}