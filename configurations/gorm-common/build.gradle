dependencies {
    compile dependencyVersion("groovy")
    compileOnly project(":inject-java")
    compile project(":inject")
    compile project(":spring")

    compile dependencyModuleVersion("gorm", "grails-datastore-gorm")
}