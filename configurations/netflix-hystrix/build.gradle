dependencies {
    compileOnly project(":inject-java")
    compile project(":configurations:netflix-archaius")
    compile dependencyVersions.hystrixCore

    compile dependencyVersions.hystrixSerialization, {
        exclude group:"com.fasterxml.jackson.module", module:'jackson-module-afterburner'
        exclude group:"com.fasterxml.jackson.core", module:'jackson-core'
        exclude group:"com.fasterxml.jackson.core", module:'jackson-databind'
        exclude group:"com.fasterxml.jackson.core", module:'jackson-annotations'
    }

    runtime dependencyVersions.jacksonAfterburner

    runtime dependencyVersions.rxJava1
    runtime project(":runtime")
    runtime dependencyVersions.rxJavaInterop

    testCompile project(":discovery-client")
    testCompile project(":inject-groovy")
    testCompile project(":inject-java")
    testCompile project(":http-server-netty")
}