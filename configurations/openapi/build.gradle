dependencies {
    compileOnly project(":inject")
    compile project(":http")
    compile project(":validation")
    compile dependencyVersion('swagger')
    compile dependencyModuleVersion('swagger', 'swagger-models')
    compile dependencyModuleVersion('swagger', 'swagger-annotations')

    testCompile project(":runtime")
    testCompile project(":inject-groovy")
    testCompile project(":inject-java-test")
    testCompile dependencyVersion("rxjava2")
}
