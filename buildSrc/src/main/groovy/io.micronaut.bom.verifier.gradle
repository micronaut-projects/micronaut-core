plugins {
    id 'java-platform'
}

configurations {
    verification {
        canBeResolved = false
        canBeConsumed = false
    }
}

def bomVerification = extensions.create('bomVerification', io.micronaut.bom.verifier.BomVerification)

tasks.register('verifyAll')
check.dependsOn verifyAll